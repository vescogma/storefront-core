import { Configuration } from '.';

export default {

  transform(config: Configuration): Configuration {

  },

  deprecationTransform(config: Configuration): Configuration {

  },

  applyDefaults(config: Configuration, defaults: Partial<Configuration>) {

  },

  validate(config: Configuration) {

  }
};
