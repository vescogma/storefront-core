import { Configuration } from '.';

const DEFAULTS: Partial<Configuration> = {
  collection: 'default',
  area: 'Production',

  stylish: false,
  initialSearch: false,
  simpleAttach: true,
  globalMixin: true
};

export default DEFAULTS;
